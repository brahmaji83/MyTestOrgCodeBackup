<apex:page controller="PracticeWrapper">
    <apex:form >
    	<apex:pageBlock >
        	<apex:pageBlockTable value="{!wrpAccSelected}" var="a">
            	<apex:column value="{!a.slno}" />
                <apex:column value="{!a.acc.Name}" />
                <apex:column value="{!a.acc.industry}" />
            </apex:pageBlockTable>
        </apex:pageBlock>
    </apex:form>
</apex:page>