<apex:page standardController="Account" recordSetVar="acc" contentType="image/gif" >
    <apex:pageBlock >
    	<apex:pageBlockTable value="{!acc}" var="a">
        	<apex:column value="{!a.name}" />
            <apex:column value="{!a.industry}" />
            <apex:column value="{!a.id}" />
        </apex:pageBlockTable>
    </apex:pageBlock>
</apex:page>